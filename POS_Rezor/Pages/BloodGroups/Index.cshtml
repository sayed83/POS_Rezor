@page
@model POS_Rezor.Pages.BloodGroups.IndexModel

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-page="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.BloodGroup[0].BloodGroupName)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.BloodGroup) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.BloodGroupName)
            </td>
            <td>
                <a asp-page="./Edit" asp-route-id="@item.BloodGroupId">Edit</a> |
                <a asp-page="./Details" asp-route-id="@item.BloodGroupId">Details</a> |
                <a asp-page="./Delete" asp-route-id="@item.BloodGroupId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
